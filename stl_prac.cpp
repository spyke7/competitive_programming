#include <bits/stdc++.h>

using namespace std;

int main()
{
    //pair
    // pair<int, pair<int, int>> arr[] = {{1, {3,4}}, {2, {5,6}}};

    // cout<<arr[1].first<<" "<<arr[1].second.second<<endl;

    // vector
    // vector<int> v(5, 20);
    // vector<int>::iterator it= v.begin();
    // cout<<*(it)<<endl;

    // for (auto c=v.begin(); c!=v.end(); c++)
    // {
    //     cout<<*(c)<<endl;
    // }

    // for (auto c:v)
    // {
    //     cout<<(c)<<endl;
    // }

    //list
    // list<int> ls;
    // ls.push_back(2); //{2}
    // ls.emplace_back(4); //{2, 4}
    // ls.push_front(3); //{3, 2, 4}
    // ls.emplace_front(); //{2,4}

    // all similar like vector
    // less costlier than vector


    // stack -> works like stack
    // stack<int> st;
    // st.push(1);
    // st.push(2);
    // st.push(5); // {5, 2,1}

    // sort
    // vector <int> a = {1,2,3,5, 4};
    // sort(a.begin(), a.end());
    // for (auto i:a){
    //     cout<<i<<endl;
    // }

    // useful functions
    // 7 -> 111
    // __builtin_popcount()
    // __builtin_popcountll() for long long

    // string permutations
    // string s = "123";
    // sort(s.begin(), s.end());
    // do {
    //     cout<<s<<endl;
    // } while (next_permutation(s.begin(), s.end()));
    
    // max
    // int maxi = *max_element(a, a+n); (start iterator, end iterator   )
    return 0;
}